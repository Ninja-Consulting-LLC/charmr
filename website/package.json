{
  "name": "charmr-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step required for static site'",
    "deploy": "gh-pages -d ."
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}

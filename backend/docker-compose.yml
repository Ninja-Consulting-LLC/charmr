version: '3'

services:
  backend:
    build: .
    ports:
      - '3001:3001'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_SANDBOX_MODE=${OPENAI_SANDBOX_MODE}

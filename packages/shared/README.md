# @charmr/shared

Shared **enums**, **plan limits**, and **generate-reply API types** for the React Native app and the Node backend.

- Build: `npm run build -w @charmr/shared` (also runs on root `npm install` via `postinstall`).
- Output: `dist/` (gitignored; generated locally and in CI).

// Mock for Image Picker
export const mockImagePicker = {
  openPicker: jest.fn(),
  openCamera: jest.fn(),
  clean: jest.fn(),
};

// Mock for react-native-paper components
export const mockPaperComponents = {
  Modal: 'Modal',
  Surface: 'Surface',
  Button: 'Button',
  IconButton: 'IconButton',
  Text: 'Text',
  Divider: 'Divider',
};
